# Data

Store raw and processed data separately. For real project data, document the
source, license, download date, activity units, preprocessing, and version.
The scaffold uses a small synthetic demonstration dataset generated by
`scripts/prepare_demo.py`.
